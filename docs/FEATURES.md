# Feature tracking

Status ∈ `todo` · `in-progress` · `review` · `done` · `parked`. One row per feature ID; one feature ID per PR. Rows are updated by `@pr-tracker` and the `track-pr` hook.

Branch stack (each PR's base is the branch below it; GitHub retargets to `main` as they merge): `main` ← `feat/F-17-api-scaffold` ← `feat/F-01-design-system` ← `feat/F-16-data-layer` ← `feat/F-09-11-test-attempt` ← `feat/F-03-06-auth-onboarding` ← `feat/F-12-13-result-solutions` ← `feat/F-02-07-08-14-shell`. `review` = implemented and reviewed in-session, PR not yet opened (GitHub repo pending).

| ID | Feature | Screens / route | Status | PR | Agent | Notes |
|---|---|---|---|---|---|---|
| F-00 | Workflow, tooling, monorepo scaffold | repo root, `.claude/`, `docs/` | done | c823868, b5c0fff, d7bf982 (main) | controller | plugins, skills, MCPs, hooks, agents, CI |
| F-01 | Design system: tokens, fonts, primitives, RTL layer, dev states screen | `packages/design-tokens`, `packages/i18n`, `apps/mobile/src/ui`, `src/app/dev/states` | review | branch `feat/F-01-design-system` | ui-builder | 20+ primitives, 51 tests, ur snapshot; code review + 2 fix rounds, i18n-rtl review, design critic clean |
| F-02 | Splash + onboarding intro (3 slides) | `src/app/(onboarding)/welcome` | review | branch `feat/F-02-07-08-14-shell` (stack tip) | ui-builder | 3-slide intro + brand plate; reviewed (code, i18n, design) + fix round clean |
| F-03 | Phone login | `src/app/(auth)/login` | review | branch `feat/F-03-06-auth-onboarding` | ui-builder | custom keypad, +91, autofill hidden input; reviewed + fix round clean |
| F-04 | OTP verification | `src/app/(auth)/otp` | review | branch `feat/F-03-06-auth-onboarding` | ui-builder | 6 cells, 24 s resend, expired/invalid/unknown branches, `<Trans>` grammar; reviewed + fix round clean; dev code hint + Use this code when the API returns `dev_code` |
| F-05 | Post selection (Constable / SI-ASI) | `src/app/(onboarding)/post` | review | branch `feat/F-03-06-auth-onboarding` | ui-builder | step 1/2, `returnTo` from Profile; reviewed + fix round clean |
| F-06 | Category selection | `src/app/(onboarding)/category` | review | branch `feat/F-03-06-auth-onboarding` | ui-builder | step 2/2, qualifying %, odd-row padding; reviewed + fix round clean |
| F-07 | Home dashboard | `src/app/(tabs)/index` | review | branch `feat/F-02-07-08-14-shell` | ui-builder | tabs (Ionicons, inset/language-aware bar), home dashboard; reviewed + fix round clean |
| F-08 | Test library | `src/app/(tabs)/tests` | review | branch `feat/F-02-07-08-14-shell` | ui-builder | library with kinds, free/locked rows, best score; reviewed + fix round clean |
| F-09 | Test attempt core | `src/app/test/[id]/index` | review | branch `feat/F-09-11-test-attempt` | ui-builder | attempt core, armed timer, usePressed primitives; reviewed (code, i18n, design) + fix round clean |
| F-10 | Question palette sheet | `src/app/test/[id]/index` | review | branch `feat/F-09-11-test-attempt` | ui-builder | six-column palette, 5 states, locked group |
| F-11 | Test dialogs, toasts, banners, call overlay | `src/app/test/[id]/index` | review | branch `feat/F-09-11-test-attempt` | ui-builder | 14 states in gallery, route-level test, haptics, back handling |
| F-12 | Result & analysis | `src/app/test/[id]/result` | review | branch `feat/F-12-13-result-solutions` | ui-builder | score/cut-off/01-02-03 sections; localised durations; reviewed + fix round clean |
| F-13 | Answers & explanation | `src/app/test/[id]/solutions` | review | branch `feat/F-12-13-result-solutions` | ui-builder | paper owns the answer key; `<Num>` filter counts; reviewed + fix round clean |
| F-14 | Profile & settings | `src/app/(tabs)/profile` | review | branch `feat/F-02-07-08-14-shell` | ui-builder | profile with token Toggle, signOut, outlined delete; reviewed + fix round clean |
| F-15 | Exam pattern config + fixtures | `packages/fixtures` | done | c823868 (main) | controller | official PWT pattern (SI split unverified); 6-question seed bank |
| F-16 | Persistence + mock API adapter | `apps/mobile/src/data` | review | branch `feat/F-16-data-layer` | ui-builder | stores, selectors, countdown, network, MockApi/HttpApi; 97 tests; review + fix round clean |
| F-17 | API scaffold: FastAPI, Postgres 17, Redis, arq scheduler, Docker Compose | `services/api`, `docker-compose.yml` | review | branch `feat/F-17-api-scaffold` (PR after repo creation) | backend-scaffolder | stubs return fixtures; 12/12 pytest; docker stack verified; `.env.example` needs manual CORS_ORIGINS edit |
| F-18 | Next.js placeholder site | `apps/web` | done | c823868 (main) | controller | landing only |
| F-19 | Guest mode: Home without login, auth on demand | `src/app/index`, `src/data/requireAuth`, `src/app/(tabs)/*`, `(auth)/*`, `(onboarding)/*` | done | main | ui-builder | boot goes straight to the shell; `useRequireAuth().ensure()` collects phone + post + category only when a paper or an account row is tapped, then returns to it; `test/[id]` redirects a deep link through the same `gateHref`; Profile offers a sign-in card when signed out |
| F-20 | Home re-layout: study / previous papers / mock cards | `src/app/(tabs)/index`, `src/features/home/HomeView` | done | main | ui-builder | the dashboard becomes a hub: the last-score card and the weak-topics list are gone; three option cards read → rehearse → sit (Study material → `/(tabs)/study` (F-21), Previous question papers → `/(tabs)/tests?kind=previous` (F-22), Mock test last as the only hi-vis action). Cards 1-2 ungated for guests and signed-in alike |
| F-21 | Study material: sections, topics, reader | `src/app/(tabs)/study`, `src/app/study/[topic]` | done | main | ui-builder | 11 topics across the 4 PWT sections in en/te/ur (`packages/fixtures/src/study.ts`); Study tab between Home and Tests; six block kinds (heading, para, bullets, formula, example, tip) with mirrored 3 px start edges; `tslprb.study` read marks; free for guests; `?kind=sectional` now steers the library shelf. te/ur copy needs a native review |
| F-22 | Previous question papers: practise or view | `src/app/paper/[id]`, `src/features/paper/PaperView`, `src/features/library/LibraryView`, `src/app/(tabs)/tests` | done | main | ui-builder | a previous-year row now offers both readings of it: Practise goes through F-19's gate to `/test/<id>`, View paper goes straight to `/paper/<id>` with no gate at all — nothing is scored, so there is nothing for an account to hold. The paper screen is one card per question: stem, the four options with a 3 px hi-vis start bar and ✓ on the key, and the explanation under a `Why` kicker; section chips jump the list. Both `prev-*` fixtures are `free` (the paywall is about mock attempts, not about a paper that was public the day it was set), and the library reads `?kind=` so Home's card lands on the right shelf even when the tab is already mounted |
| F-23 | Home v3: exam countdown, updates, physical check, affairs, progress | `src/app/(tabs)/index`, `src/features/home/*`, `src/data/{activity,history,useNow}`, `packages/fixtures/src/exam-info.ts` | done | main | ui-builder | Home stops repeating the tab bar. Five blocks: a hazard-framed countdown hero (days to `EXAM_INFO.pwtDate`, streak, a today's-target bar of ten `View` blocks filled from `tslprb.activity`), no Continue card (removed at the user's request 2026-09-03); Check eligibility is the only hi-vis action, a horizontal shelf of TSLPRB notices, a PMT/PET "check eligibility" card, three current-affairs rows, and three progress tiles (topics read n/N, papers practised, best score) with a guest nudge. `activity.bump()` is called from the attempt route's answer handler and the topic route's mark-read, and `history.record()` from the attempt route after the API scores the paper — no store imports another store. Study/Previous papers/Mock cards and their strings are gone. The notice shelf and the affairs rows are the three newest of F-24's fixtures (`latestNotices(3)` / `latestAffairs(3)`), chipped with F-24's own `updates.kind.*` and `affairs.cat.*` labels; `src/features/home/dates.ts` keeps Home's numeric `DD-MM` / `DD-MM-YYYY` forms, which are shorter than the `/updates` ones. every Home block now links to a route that exists. Fix wave 1 (review F-23-25): the best-score tile is a percentage of the paper's marks (`bestPercent`, ranked by share so 15/15 beats 120/200), the hero is a fact rather than a button and reads "Exam day" on the day and "PWT held on <date>" after it (`daysUntil` goes negative), the streak count is an isolated digit with a singular form, a tapped notice opens `/updates?open=<id>`, empty shelves are not drawn, the Urdu notice shelf starts at the newest notice, `useNow` also refreshes on focus, and the study bookmark (`lastRead`) is gone |
| F-24 | TSLPRB updates + current affairs | `src/app/updates`, `src/app/affairs`, `src/features/news/*`, `packages/fixtures/src/{notices,affairs}.ts` | done | main | ui-builder | two ungated reading screens the Home blocks link to. `/updates` is the Board's notice board: six seeded notices for a 2026 cycle (notification, application window, previous final list, PWT date, hall tickets, PMT/PET) with a kind chip, a `<Num>` date, and the body behind a tap (`accessibilityState.expanded`, mirrored caret) plus a link row to tslprb.in where there is one. `/affairs` is the daily digest: ten seeded items grouped by day under a `<Num>` date kicker, each card kickered `sand` with its category. Dates go through a `Date`-free `d MMM yyyy` helper — `new Date('2026-08-04')` is UTC midnight and would date a notice a day early west of Greenwich. Both fixtures are INVENTED sample data, marked as such in a header comment, and must be swapped for `GET /notices` and `GET /affairs` before launch; te/ur copy needs a native review. Until then a static dim "Sample data" `Chip` (`common.sampleData`, `testID="sample-data"`) sits at the end of the heading row on Home's updates and affairs rows and in the `/updates` and `/affairs` headers (`BackHeader` gained a `trailing` slot for it); it mirrors with `useDir()` and is never pressable (now `Chip tone="label"`). Fix wave 1: `/updates` takes `?open=<id>` and arrives with that notice expanded; opening a notice is a 180 ms layout transition with a turning caret; the empty states are centred with a "Nothing yet" kicker; affairs cards carry the same 3 px sand start edge as on Home and the day heading is chalk2 |
| F-25 | PMT/PET eligibility checker | `src/app/eligibility`, `src/features/eligibility/*`, `packages/fixtures/src/physical-standards` | done | main | ui-builder | "do I meet the physical standards?" answered before the trip to the ground: post (Constable/SI) x gender x category group (General / ST-agency) pickers, decimal-pad fields for height, chest + expansion (men only), the post's run events (1600 m constable men, 800 m constable women, 100 m + 800 m SI), long jump and shot put, and a pure `evaluate()` that returns a row per standard with required vs yours plus one verdict (eligible / not yet + what to work on / incomplete). Free for guests, prefilled from the session's post and category without writing back to it, and the last entry persists in `tslprb.eligibility`. The standards table was researched against the 2022 notification on 2026-09-03: the Constable PMT (167.6 / 86.3 + 5 cm men, 152.5 cm women, ST 160 / 150 cm) and PET (1600 m in 435 s men / 800 m in 320 s women, long jump 4.00 / 2.50 m, shot put 6.00 / 4.00 m) are confirmed; the ST men's chest (80 + 3 cm) and every SI figure are unconfirmed, tagged "Unconfirmed" on their result rows, with a note under the pickers whenever the table has an unconfirmed figure (`allVerified`) and the disclaimer under every verdict. Fix wave 1: the 1600 m / 800 m are typed as minutes and seconds in two number-pad fields (`runTime.ts` joins them into the stored seconds; "7.15" can no longer pass as 7.15 s) and the limit prints as `7:15`; the 100 m stays a seconds field; every empty field shows its own standard as a placeholder; the run labels and the disclaimer year are `iso()` interpolations, not digits in locale strings; the pickers are quiet, full-width `SegmentedChips`; result rows have a mark column, two fixed figure columns and hairline rules; Check scrolls to the verdict |
| F-27 | Docker: app on :3201, API on :8200 | `docker-compose.yml`, `apps/mobile/Dockerfile`, `apps/mobile/nginx.conf`, `.dockerignore`, `services/api`, root `package.json`, `.github/workflows/ci.yml` | done | main | backend-scaffolder | `pnpm docker:up` (= `docker compose up -d --build`) runs five services with no compose profiles: `web` — Expo `expo export --platform web` (static output) behind `nginx:alpine`, multi-stage from node:22-alpine + pnpm 11.25.0 with the repo root as build context so the `@tslprb/*` workspace packages resolve, nginx `try_files $uri $uri.html $uri/index.html /index.html`, gzip, no-cache HTML / immutable hashed bundles, the COEP/COOP headers expo-sqlite's worker needs — on `${WEB_HOST_PORT:-3201}`; `api` on `${API_HOST_PORT:-8200}` (uvicorn 8000 inside, `--reload` over the bind mount, `/health` healthcheck, `CORS_ORIGINS` defaulting to `http://localhost:${WEB_HOST_PORT}` + the Expo dev origins); `worker`; postgres:17 on 5442; redis:7 on 6379. The web image bakes `EXPO_PUBLIC_API=http` and `EXPO_PUBLIC_API_URL=http://localhost:${API_HOST_PORT}` (the browser calls the host port, not the service name). Root `.dockerignore` + `services/api/.dockerignore` (the api image no longer copies the host `.venv`). Scripts `docker:up/down/logs/build`; `api:up/down/logs/migrate` lost `--profile dev`. CI job `docker` runs `docker compose build web api`. `HttpApi` now serves `listTestMetas`/`getTestMeta`/`getPaper` from the in-app fixture bank (like `getResultDetail`) instead of rejecting 501, so the Dockerised app is playable end to end; OTP goes over the wire; the attempt lifecycle is attempted and 404s today (fixture ids differ, see below). Known gap for F-17: the API's fixture ids (`test-pwt-07`) differ from the app's (`mock-07`), so `POST /v1/attempts` 404s and the attempt runs on the local clock. The web Dockerfile uses plain syntax (no `RUN --mount` cache, no `# syntax` pin) so a host whose Compose lacks the buildx plugin and falls back to the legacy builder builds it too. Public URL (2026-09-05): nginx proxies `/api/` to the api service so the browser talks to one origin; `PUBLIC_URL` (default `http://localhost:3201`) drives the baked `EXPO_PUBLIC_API_URL=${PUBLIC_URL}/api` and the `CORS_ORIGINS` default; `docker-compose.prod.yml` (`pnpm docker:prod`) pins `https://mocktest.brollyexamprep.com` and `restart: unless-stopped`; TLS terminates in a reverse proxy on the server that forwards to 127.0.0.1:3201 |
| F-28 | Brolly rebrand, Phase A: tokens, fonts, primitives, chrome, brand | `packages/design-tokens`, `packages/i18n`, `apps/mobile/src/ui`, `src/app/_layout`, `(tabs)/_layout`, `assets/brand`, `app.json`, `apps/web`, docs | done | main — six commits (A1, A2, A3a, A3b, A4, A5) + fix wave 1 | ui-builder | The app takes Brolly Solutions' identity (spec `docs/specs/2026-09-05-brolly-rebrand-design.md`): semantic colour tokens on cream (`canvas`/`surface`/`ink`/`accent`/`ok`/`danger` + tints, raw scale in `palette`) with every hi-vis name aliased in `legacyColors` so the 69 screen files compile untouched; `test/contrast.test.mjs` pins every text pair at AA/AAA and `test/legacy.test.mjs` bans legacy names from `src/ui`. Inter + Playfair Display replace Archivo (`typography(lang, role, weight, { italic, numeric })` routes title/titleLg/display/wordmark to Playfair in en, Noto 700 in te, digits always Inter). Every primitive rewritten on semantic names: ink primary button, red outline danger, gold outline accent; surface cards with a warm shadow, `trailing` slot and `flat`; chip tones `accent`/`danger`/`ok`/`label`; `Rail` (3 px gold, red + pulse when critical) replaces `HazardRail`; palette cells gold = answered, ink = marked, red outline = unanswered; round gold toggle; floating toasts with an `info` tone; surface dialog/sheet with a 3 px gold edge; surface tab bar. `Brand` lockup (umbrella + live Playfair wordmark) on Home, Login and the gallery, the full logo on Welcome; `common.brand` = "Brolly Solutions". Light `app.json`: cream splash with the logo, navy/gold "B" adaptive icon, template assets and `make-splash-icon.mjs` deleted, `make-brand-assets.mjs` (sharp) derives every raster. Pressed feedback on cream is a `surface2` fill. Docs rewritten (`DESIGN_SYSTEM.md` "Brolly on cream", rules, CLAUDE.md). Fix wave 1 (review): `outline` + `okTint` tokens, rgba compositor in the contrast test, ramp caption 12 / small 13 / body 15 / bodyLg 16 / question 17.5, Noto Serif Telugu for te display roles, tab labels on a 16/19 line, chip/segment 2 px gold bottom edge, disabled primary surface2 + outline + ink3, one red toast, `cloud-offline` banner icon, selected cards keep press feedback, rasters in brand ink with a bleed, `sharp` pinned, ink text on tints, quiet dividers, one gold edge on Home, green eligible verdict, `it.failing` guards for Phase C, `app.json` identity, `npx expo-doctor`. 791 mobile tests (89 suites, 16 snapshots); 43 token tests (contrast 39, scale 3, legacy 1); all 16 snapshots refreshed once more. Follow-ups: trace the umbrella to SVG, Phases B–E |
| F-26 | Two languages: Urdu removed | `packages/i18n`, `packages/design-tokens`, `packages/api-contracts`, `packages/fixtures`, `apps/mobile`, `services/api`, docs | done | main | ui-builder | User ruling 2026-09-03: English + Telugu only. `Lang` is `'en' \| 'te'` in i18n, design-tokens, api-contracts, fixtures (`Localized = { en, te }`) and the API (`schemas.Lang`, `fixtures.py`); `ur.json`, the `lang.ur`/`urShort` keys, the Noto Nastaliq Urdu font package and `useFonts` entry, the `ur` token row, tab-bar height and `bodyDelta`/`minSize` branches are gone; every language switcher offers EN / తె; `?lang=ur` falls back to English. The direction layer (`useDir`, `dir`, `isRTL`, `Num`, `iso`, `Row physical`, `startEdge`) is untouched and dormant (`RTL_LANGS` is empty). Six `*.ur.test.tsx` snapshot tests became `*.te.test.tsx` (Telugu copy in the `NotoSansTelugu` face); every other `(ur)` block renders in `te`; `row-reverse` assertions removed. Parity test compares en ↔ te. Docs, rules, agent and templates say two languages |

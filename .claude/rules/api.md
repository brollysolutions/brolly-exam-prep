---
paths:
  - "services/api/**"
  - "packages/api-contracts/**"
  - "docker-compose.yml"
---
# API rules (auto-loaded)

- FastAPI + SQLAlchemy 2 (async) + Alembic + pydantic v2. Python 3.12 in Docker. Tooling: `uv`, `ruff`, `pytest`.
- Every request/response model has a matching zod schema in `packages/api-contracts`; regenerate types with `pnpm contracts:gen` (openapi-typescript) after changing routes.
- Scheduler = `arq` worker on Redis. Cron jobs live in `services/api/app/worker.py`. No Celery.
- Migrations are autogenerated then reviewed; never edit applied migrations.
- Sign-in is `POST /v1/auth/phone`: the number alone, no OTP and no password (removed 2026-09-07 at the product owner's request). It creates the account on first sight of a number, so nothing proves possession of it — do not put anything behind this gate that a stranger may not see. Users and tokens live in process-local dicts, so a restart signs everyone out.
- Secrets from `.env` only; commit `.env.example`.
- Docker (F-27): `pnpm docker:up` runs the whole app — `web` (Expo static export behind nginx) on http://localhost:3201, `api` on http://localhost:8200 (uvicorn on 8000 inside, `--reload` over the bind mount), `worker`, postgres:17 on 5442, redis:7 on 6379. No compose profiles. `pnpm api:up` starts only `postgres redis api worker`; `pnpm api:migrate` = `alembic upgrade head` in the container. Host ports are `WEB_HOST_PORT` / `API_HOST_PORT` / `POSTGRES_HOST_PORT` / `REDIS_HOST_PORT`; the api `CORS_ORIGINS` default follows `WEB_HOST_PORT`, the web image bakes `EXPO_PUBLIC_API_URL` from `API_HOST_PORT` (rebuild `web` after changing it).

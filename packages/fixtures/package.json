{
  "name": "@tslprb/fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Exam pattern config, question bank (en/te) and sample results used by the mock API and tests.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
